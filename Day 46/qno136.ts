// Question 136: Use console.log() to debug and track the value of a variable inside a loop.


for (let i = 1; i <= 5; i++) {
    let variable = i * 2;
    console.log(`Value of i ${i}, Iteration: ${variable}`)   
  }
